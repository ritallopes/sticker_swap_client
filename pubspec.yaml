name: sticker_swap_client
description: Aplicativo para gerenciar álbum da copa 2022.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <4.0.0'


dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  firebase_core: ^2.3.0
  cloud_firestore: ^4.8.1
  firebase_auth: ^4.6.3

  cupertino_icons: ^1.0.2
  http: ^0.13.5

  qr_flutter: ^4.1.0
  flutter_speed_dial: ^6.2.0
  flutter_barcode_scanner: ^2.0.0

  rxdart:
  dio: ^4.0.6
  flutter_modular: ^5.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  mockito: ^5.4.2
  build_runner: ^2.4.6


flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/bandeiras/
